============
Installation
============

At the command line::

    $ easy_install {{ rubick.repo_name }}

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv {{ rubick.repo_name }}
    $ pip install {{ rubick.repo_name }}
